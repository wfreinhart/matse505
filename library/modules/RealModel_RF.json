{
  "id": "RealModel_RF",
  "type": "Model",
  "parent_lecture": "",
  "requirements": [],
  "markdown_content": "# Random Forest Regressor\nRandom Forest with max_depth=5 as suggested in Lecture 03.",
  "code_block": "def run_module(ctx):\n    import numpy as np\n    import pandas as pd\n    from matplotlib import pyplot as plt\n    from scipy import stats\n    import sklearn\n    from sklearn import metrics, model_selection, linear_model, cluster, decomposition, preprocessing, ensemble, tree, neighbors, svm, mixture\n    from sklearn.ensemble import RandomForestRegressor\n    model = RandomForestRegressor(max_depth=5, random_state=42)\n    ctx['model'] = model",
  "raw_code": "from sklearn.ensemble import RandomForestRegressor\nmodel = RandomForestRegressor(max_depth=5, random_state=42)"
}