{
  "id": "Lecture02_statistical_moments",
  "markdown_content": "## Statistical moments\n\nWe might be interested in some higher moments of data.\nHere are the skew and kurtosis for our `Series`:\n\nThese can be computed manually using the definition for [standardized moments](https://en.wikipedia.org/wiki/Standardized_moment):\n\n$\\bar{\\mu}_3 = E[(X-\\mu)^3] / \\sigma^3$\n\n$\\bar{\\mu}_4 = E[(X-\\mu)^4] / \\sigma^4$\n\nwhere $\\mu$ is the mean and $\\sigma$ is the standard deviation.\n\nYou will notice that the skew is pretty close, but the kurtosis is way off (even a different sign).\nWe can look at the documentation for `kurt` to find out why:\n\nThe Fisher's definition of kurtosis uses a normal distribution as the baseline, so we need to subtract 3:",
  "code_block": "\n\nprint(x.skew())\nprint(x.kurt())\n\nmu = np.mean(x)\nsigma = np.std(x)\nskew = np.mean((x-mu)**3) / sigma**3\nprint(skew)\n\nkurt = np.mean((x-mu)**4) / sigma**4\nprint(kurt)\n\nhelp(x.kurt)\n\nkurt_fisher = np.mean((x-mu)**4) / sigma**4 - 3\nprint(kurt_fisher)",
  "imports": [
    "import requests",
    "import pandas as pd",
    "import numpy as np",
    "import os",
    "from matplotlib import pyplot as plt  # use an alias",
    "from matplotlib import pyplot  # import the pyplot submodule",
    "from scipy import stats"
  ],
  "cells": [
    {
      "type": "markdown",
      "content": "## Statistical moments\n\nWe might be interested in some higher moments of data.\nHere are the skew and kurtosis for our `Series`:",
      "metadata": "# %% [markdown]"
    },
    {
      "type": "code",
      "content": "print(x.skew())\nprint(x.kurt())",
      "metadata": "# %%"
    },
    {
      "type": "markdown",
      "content": "These can be computed manually using the definition for [standardized moments](https://en.wikipedia.org/wiki/Standardized_moment):\n\n$\\bar{\\mu}_3 = E[(X-\\mu)^3] / \\sigma^3$\n\n$\\bar{\\mu}_4 = E[(X-\\mu)^4] / \\sigma^4$\n\nwhere $\\mu$ is the mean and $\\sigma$ is the standard deviation.",
      "metadata": "# %% [markdown]"
    },
    {
      "type": "code",
      "content": "mu = np.mean(x)\nsigma = np.std(x)\nskew = np.mean((x-mu)**3) / sigma**3\nprint(skew)\n\nkurt = np.mean((x-mu)**4) / sigma**4\nprint(kurt)",
      "metadata": "# %%"
    },
    {
      "type": "markdown",
      "content": "You will notice that the skew is pretty close, but the kurtosis is way off (even a different sign).\nWe can look at the documentation for `kurt` to find out why:",
      "metadata": "# %% [markdown]"
    },
    {
      "type": "code",
      "content": "help(x.kurt)",
      "metadata": "# %%"
    },
    {
      "type": "markdown",
      "content": "The Fisher's definition of kurtosis uses a normal distribution as the baseline, so we need to subtract 3:",
      "metadata": "# %% [markdown]"
    },
    {
      "type": "code",
      "content": "kurt_fisher = np.mean((x-mu)**4) / sigma**4 - 3\nprint(kurt_fisher)",
      "metadata": "# %%"
    }
  ],
  "sequence": 32,
  "parent_lecture": "Lecture02"
}