{
  "id": "Lecture06_where_manifold_learning_fails",
  "markdown_content": "## Where manifold learning fails\n\nLet's revisit our composition data from above.\n\nWe can try `SpectralEmbedding` again:\n\nThe `nearest_neighbors` affinity does not seem to be working.\nLet's go back to `rbf`:\n\nThis is almost identical to PCA.\nWhat about diffusion maps?\n\nYou can see that depending on the choice of $\\varepsilon$ we will get wildly different results, eventually ending up back at something like the linear result.",
  "code_block": "\n\nx = data.loc[:, ' C':'Nb + Ta']\n# here's a one-liner to encode the str labels as int:\n_, y = np.unique([it[0] for it in data['Alloy code']], return_inverse=True)\n\nZ = manifold.SpectralEmbedding(random_state=0).fit_transform(x)\n\nfig, ax = plt.subplots()\nax.scatter(Z[:, 0], Z[:, 1], c=y)\nax.set_xlabel('$Z_0$')\nax.set_ylabel('$Z_1$')\n\nZ = manifold.SpectralEmbedding(affinity='rbf', random_state=0).fit_transform(x)\n\nfig, ax = plt.subplots()\nax.scatter(Z[:, 0], Z[:, 1], c=y)\nax.set_xlabel('$Z_0$')\nax.set_ylabel('$Z_1$')\n\ndist = distance.squareform(distance.pdist(x))\n\nepsilon = np.percentile(dist, 100)\nL = np.exp(-dist**2/epsilon**2)\n\nD = np.diag(np.sum(L, axis=1))\nP = np.linalg.inv(D) @ L\n\nw, v = np.linalg.eig(P)\nplt.scatter(*v[:, 1:3].T, c=y)",
  "imports": [
    "import pandas as pd",
    "import numpy as np",
    "from sklearn import manifold",
    "from sklearn import semi_supervised",
    "from sklearn import preprocessing",
    "from sklearn import decomposition",
    "import umap",
    "import os",
    "from sklearn import datasets",
    "from plotly import express as px",
    "from matplotlib import pyplot as plt",
    "from scipy.spatial import distance"
  ],
  "cells": [
    {
      "type": "markdown",
      "content": "## Where manifold learning fails\n\nLet's revisit our composition data from above.",
      "metadata": "# %% [markdown]"
    },
    {
      "type": "code",
      "content": "x = data.loc[:, ' C':'Nb + Ta']\n# here's a one-liner to encode the str labels as int:\n_, y = np.unique([it[0] for it in data['Alloy code']], return_inverse=True)",
      "metadata": "# %%"
    },
    {
      "type": "markdown",
      "content": "We can try `SpectralEmbedding` again:",
      "metadata": "# %% [markdown]"
    },
    {
      "type": "code",
      "content": "Z = manifold.SpectralEmbedding(random_state=0).fit_transform(x)\n\nfig, ax = plt.subplots()\nax.scatter(Z[:, 0], Z[:, 1], c=y)\nax.set_xlabel('$Z_0$')\nax.set_ylabel('$Z_1$')",
      "metadata": "# %%"
    },
    {
      "type": "markdown",
      "content": "The `nearest_neighbors` affinity does not seem to be working.\nLet's go back to `rbf`:",
      "metadata": "# %% [markdown]"
    },
    {
      "type": "code",
      "content": "Z = manifold.SpectralEmbedding(affinity='rbf', random_state=0).fit_transform(x)\n\nfig, ax = plt.subplots()\nax.scatter(Z[:, 0], Z[:, 1], c=y)\nax.set_xlabel('$Z_0$')\nax.set_ylabel('$Z_1$')",
      "metadata": "# %%"
    },
    {
      "type": "markdown",
      "content": "This is almost identical to PCA.\nWhat about diffusion maps?",
      "metadata": "# %% [markdown]"
    },
    {
      "type": "code",
      "content": "dist = distance.squareform(distance.pdist(x))\n\nepsilon = np.percentile(dist, 100)\nL = np.exp(-dist**2/epsilon**2)\n\nD = np.diag(np.sum(L, axis=1))\nP = np.linalg.inv(D) @ L\n\nw, v = np.linalg.eig(P)\nplt.scatter(*v[:, 1:3].T, c=y)",
      "metadata": "# %%"
    },
    {
      "type": "markdown",
      "content": "You can see that depending on the choice of $\\varepsilon$ we will get wildly different results, eventually ending up back at something like the linear result.",
      "metadata": "# %% [markdown]"
    }
  ],
  "sequence": 16,
  "parent_lecture": "Lecture06"
}